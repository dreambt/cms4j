<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CMS">

    <!--
         获取文章数量
     -->
    <select id="getTopTen" resultType="Archive">
        select id, title, article_count as articlecount, create_time as createtime, modify_time as modifytime
        from cms_archive
        order by create_time desc
        limit 0,10
	</select>

</mapper>